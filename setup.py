from setuptools import setup

setup(
    name='mempy', # what you pip install
    version='0.1',
    description='Behavioral GPU simulations of device characteristics in ML algorithms',
    py_modules='mempy',
    package_dir={'': 'mempy'}
)